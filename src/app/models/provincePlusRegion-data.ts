export interface ProvincePlusRegionData{
    province: string,
    health_region: string,
    date: string,
    newCases: number,
    cumulative_cases: number,
    newDeaths: number, 
    cumulative_deaths: number
}